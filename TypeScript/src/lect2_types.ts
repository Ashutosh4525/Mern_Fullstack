let drink ="chai";
//in ts if value given to variable then it inferece the dataType automatically without specifying
//It is applied to all Primitive dataTypes 

let cups=Math.random()>0.5 ? 10 : '5';
// this would be refrence to string and number both
//this is type inferencing

let chaiFlavour:string="masala chai"
//:string is an annotation noting but declaring the datatype of the variable